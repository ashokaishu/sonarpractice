pipeline{
    agent {label 'linux'}
    options{
        buildDiscarder(logoRotator(numToKeeStr:'5'))
    }
    stages{
        stage('scan'){
            steps{
                withSonarQubeEnv(installationName: 'sonarqube'){
                    sh './mvnw clean org.sonarsource.scanner.maven:sonar-maven-plugin:3.9.0.2155:sonar'
                }
            }
        }
    }
}